# Copy to .env and fill in. Must match backend/.env's WHATSAPP_SIDECAR_SHARED_SECRET
# exactly (used to authenticate every call between this sidecar and the FastAPI
# backend, in both directions) — pick any long random string.
LEADPILOT_BACKEND_URL=http://127.0.0.1:8000
SIDECAR_INTERNAL_SECRET=changeme-shared-with-backend
SIDECAR_PORT=8020

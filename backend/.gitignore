# secrets — real API keys/DB password live here, never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# virtual environment
venv/
.venv/

# whatsapp_sidecar/ is a Node subproject (SKILL-MULTI-TENANT-CONNECT.md §3) — its
# own dependencies/logs, never committed, same as everything else here
node_modules/
*.log

# OS
.DS_Store
